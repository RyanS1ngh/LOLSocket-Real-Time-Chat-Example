const disconnectOnClose = (websocket, callback) => {
    websocket.onclose = (event) => {
      console.log(`WebSocket closed with code ${event.code}. Reconnecting...`);
      callback();
    };
  };
  
  module.exports = disconnectOnClose;
  